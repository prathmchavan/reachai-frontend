import { RegisterForm } from "@/components";
import { Box } from "@mui/material";

const Page = () => {
    return(
        <Box>
            <RegisterForm />
        </Box>
    )
};

export default Page;